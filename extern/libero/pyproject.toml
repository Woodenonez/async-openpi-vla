[project]
name = "libero"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "bddl>=3.6.0",
    "robosuite==1.5.1",
    "easydict>=1.13",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["libero"]